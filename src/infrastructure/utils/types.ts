export interface RecipeFiltersType {
    difficulty?: string; // o un enum se hai un set fisso di difficoltà
    prepTime?: string; // o un numero se rappresenta i minuti
    category?: string; // o un enum se hai un set fisso di categorie
  }